# =============================================================================
# FedRGBD Python Dependencies
# =============================================================================
# NOTE: PyTorch must be installed separately on Jetson (see setup_jetson.sh)
#       The NVIDIA-specific wheel is required for CUDA support on ARM64.
#       Do NOT install torch/torchvision from PyPI on Jetson.
# =============================================================================

# Federated Learning
flwr[simulation]==1.13.1

# Data & ML (PyTorch installed separately on Jetson)
scikit-learn>=1.3.0
scipy>=1.11.0
numpy==1.26.4              # MUST be 1.26.x — numpy 2.x breaks PyTorch on Jetson

# Statistical Analysis
pingouin>=0.5.4

# Data Processing
pandas>=2.0.0
Pillow>=10.0.0
opencv-python-headless>=4.8.0

# Configuration
pyyaml>=6.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Utilities
tqdm>=4.65.0
